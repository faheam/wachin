<UserControl x:Class="Wachin.Views.AutomationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:serv="clr-namespace:Wachin.Services">
    <Grid Margin="20,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Automatizacion y Mantenimiento" Style="{StaticResource H1}"/>
            <TextBlock Text="Ejecuta scripts de mantenimiento, reparacion y limpieza del sistema" Style="{StaticResource Body}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Scripts List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ScriptsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <!-- Title and Risk -->
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="13"
                                                   Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                                        <Border Background="{Binding RiskColor}" CornerRadius="4" Padding="6,2" Margin="8,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding RiskLabel}" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                                        </Border>
                                    </StackPanel>

                                    <!-- Description -->
                                    <TextBlock Text="{Binding Description}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,6"
                                               Foreground="{DynamicResource TextSecondary}"/>

                                    <!-- Info row -->
                                    <StackPanel Orientation="Horizontal">
                                        <Border Background="{DynamicResource CreamChip}" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                                            <TextBlock Text="{Binding CategoryLabel}" FontSize="10" Foreground="{DynamicResource CreamChipText}" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border Background="{DynamicResource CreamChip}" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                                            <TextBlock Text="{Binding TimeLabel}" FontSize="10" Foreground="{DynamicResource CreamChipText}" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border Background="{DynamicResource CreamChip}" CornerRadius="4" Padding="6,2">
                                            <TextBlock Text="{Binding AdminLabel}" FontSize="10" Foreground="{DynamicResource CreamChipText}" VerticalAlignment="Center"/>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Run Button -->
                                <Button Grid.Column="1" Content="{Binding ButtonText}" Style="{Binding ButtonStyle}"
                                        Padding="16,10" VerticalAlignment="Center" Margin="16,0,0,0"
                                        Click="RunScript_Click" Tag="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Status Bar -->
        <Border Grid.Row="2" Style="{StaticResource Card}" x:Name="StatusBar" Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusIcon" Text="*" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center" FontWeight="Bold"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="StatusText" Text="" FontSize="12"/>
                    <ProgressBar x:Name="ProgressBar" Height="4" Margin="0,6,0,0" Minimum="0" Maximum="100" Value="0"
                                 Foreground="{DynamicResource Accent}" Background="{DynamicResource BgChip}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
